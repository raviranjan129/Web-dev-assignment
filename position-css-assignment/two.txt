 //position :Absolute;
 
{
    position:absolute;
}
 <1> with relative positioning , an element can be moved up,down,left,or right from its normal positon.
<2> it will not break the normal document flow to position the element on the page.
    
 



// position Relative;
 
    {
        position:relative;
    }

    <1> with absolute positioning an element is positioned relative to its nearest positioned ancestor.

<2> it will break the normal document flow to position the element on the page.

 